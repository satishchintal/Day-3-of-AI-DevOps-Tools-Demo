const test=require('node:test');const assert=require('node:assert/strict');const{clamp,overlaps}=require('../src/game.js');test('clamp',()=>{assert.equal(clamp(-1,0,10),0);assert.equal(clamp(5,0,10),5);assert.equal(clamp(11,0,10),10)});test('collision',()=>{assert.equal(overlaps({x:0,y:0,w:20,h:20},{x:10,y:10,w:20,h:20}),true);assert.equal(overlaps({x:0,y:0,w:20,h:20},{x:30,y:30,w:20,h:20}),false)});
